import { FormContainer } from './FormContainer'
import { FormMain } from './FormMain'

export const LoginForm = () => {
    return (
        <FormContainer>
            <FormMain />
        </FormContainer>
    )
}
